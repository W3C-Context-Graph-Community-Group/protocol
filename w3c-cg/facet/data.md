# facet/data

**Data comparison — the raw value**

- **Key:** `https://w3c-cg.github.io/context-graph/facet/data`
- **Dependency:** Valid only when μ_C = 1, μ_M = 0, μ_S = 0
- **Indicator:** μ_D ∈ {0, 1} — 0 = match, 1 = mismatch

Data comparison is the residual check: do values agree once meaning and structure are verified to be shared? When all four indicators resolve to Act, **the boundary is coherent**.

---

Part of the [Context Graph](https://w3c-cg.github.io/context-graph/).
